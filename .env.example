# 设备 SSH 凭据配置
# 将这些环境变量设置为实际的密码值

# 默认设备 SSH 密码
DEVICE_DEFAULT_PASSWORD=

# 防火墙设备后台 root 密码
DEVICE_BACKEND_PASSWORD=

# 其他设备后台 root 密码
DEVICE_BACKEND_PASSWORD_OTHER=

# 默认 SSH 用户名（可选，默认为 admin）
DEVICE_DEFAULT_USER=admin
